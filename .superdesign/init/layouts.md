# Layouts

No central layout components yet.

Web library uses `.shell`, `.hero`, `.dashboard`, `.panel`, and `.toolbar` in `apps/web/src/style.css`.
Extension popup uses `.popup`, `.summary`, `.highlights`, `.auth-panel`, and `.status` in `apps/extension/entrypoints/popup/App.css`.
